kind = "web"
previewPath = "/"
title = "VendorBridge Procurement ERP"
version = "1.0.0"
id = "artifacts/vendorbridge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21613

[services.development]
run = "pnpm --filter @workspace/vendorbridge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vendorbridge", "run", "build" ]
publicDir = "artifacts/vendorbridge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21613"
BASE_PATH = "/"
